export const dynamic = 'force-dynamic';

export default function InboxPage() {
  // Layout renders DashboardShell with initialView="inbox", which routes the
  // shell's view switch to <InboxView />. This page intentionally renders
  // nothing of its own.
  return null;
}
